linked list library (double link, tree compatibility)
	merge sort, etc.
trees
	every manager has employees. each employee has an employee rating. Each manager has a rating equal to the average of their employees.
	Binary search
heap stuff